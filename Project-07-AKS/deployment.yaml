apiVersion: apps/v1
kind: Deployment
metadata:
  name: aks-nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aks-nginx
  template:
    metadata:
      labels:
        app: aks-nginx
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80

